#!/usr/bin/env bash
set -euo pipefail

# shellcheck source=common
source "$(dirname "${BASH_SOURCE[0]}")/common"
ensure_runtime_dirs
"$(dirname "${BASH_SOURCE[0]}")/main" start || true
echo "Docker Manager upgrade callback"
